import React from 'react';

const MainText = ({text}) => {
    return (
        <div>
            <h2 className='text-[35px] uppercase leading-9 font-bold mt-[57px]'>{text}</h2>
        </div>
    );
}

export default MainText;

// 
